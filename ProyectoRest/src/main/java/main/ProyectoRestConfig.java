package main;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;
import org.springframework.context.annotation.Configuration;

/**
 * The Class ProyectoRestConfig.
 */
@Configuration
public class ProyectoRestConfig {
	
	/** Constante _logger. */
	static final Logger _logger = LogManager.getLogger(ProyectoRestConfig.class);
}
